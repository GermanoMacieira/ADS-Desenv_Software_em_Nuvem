
    const algoritmo = require('./algoritmos');

    // Teste 01: ordenação com um números inteiros
    test('Ordenando uma sequencia aleatório de números', () => {
        const quickSort = algoritmo.quickSort(???);
        expect(quickSort).toBe(???);
    });

    // Teste 02: 